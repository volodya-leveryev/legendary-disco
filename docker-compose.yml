version: "3.3"

services:

  app:
    build: .
    restart: always
    environment:
      JIMMY_CONFIG: production.cfg
    ports:
      - 127.0.0.1:5000:5000
    volumes:
      - ./jimmy:/jimmy

  db:
    image: mongo
    restart: always
    volumes:
      - ./db:/data/db
